/**
 * Mobile layout constants for consistent spacing across screens.
 */

// Bottom navigation
export const BOTTOM_NAV_HEIGHT = 64;
export const BOTTOM_NAV_OFFSET = `${BOTTOM_NAV_HEIGHT}px`;

// Container padding
export const CONTAINER_PADDING = '10px';

// Standard gap values (pixel strings for sx prop)
export const GAP = {
  xs: '4px',
  sm: '8px',
  md: '12px',
  lg: '16px',
} as const;
